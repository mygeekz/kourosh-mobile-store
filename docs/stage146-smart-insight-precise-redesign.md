# Stage 146 — Precise Smart Insight redesign

Fixes the exact reported parts:
- Command card icon is no longer black/missing.
- Status/progress inside `فرمان مدیریتی امروز` uses standard blue/green treatment.
- Next Best Decision card is compact and no longer leaves a large empty middle.
- Black priority chips, impact/risk chips, and action links are replaced with neutral blue chips/buttons.
- Adds explicit JSX classes instead of only broad CSS overrides.

Files:
- `pages/reports/SmartInsightCenter.tsx`
- `styles/runtime-overrides/11f-smart-insight-precise-redesign.css`
- `index.tsx`
