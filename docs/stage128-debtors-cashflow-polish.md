# Stage 128 — Debtors + Cashflow polish

Debtors:
- Search icon moved to physical left.
- Search input padding fixed.
- Hero made solid and cleaner.
- KPI cards made more readable.
- Table text wraps safely and no longer forces unnecessary nowrap.
- Back link route fixed from `/#/reports` to `/reports`.

Cashflow:
- Filter controls moved out of PageKit header to a standalone filter card.
- This prevents the page title/search/header from being squeezed.
- Date presets, date pickers, forecast select, and refresh button now have a stable responsive grid.
- Charts remain unchanged.

Note:
Cashflow currently has two charts: selected-period actual cashflow and forecast cashflow. That is enough for a minimal operational page, but not enough for a full financial decision dashboard.
