// Backward-compatible entry point. The project now synchronizes ExcelJS together
// with the reviewed Router, PWA, Recharts, source-map and isolated glob chains.
await import('./prepare-reviewed-dependency-chain.mjs');
