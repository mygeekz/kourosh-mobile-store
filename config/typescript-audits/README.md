# TypeScript Audit Configs

These files are audit-only TypeScript configurations. They were moved out of the project root to keep the root clean while preserving audit commands.

Do not delete them unless the matching `package.json` scripts are removed or rewritten.
