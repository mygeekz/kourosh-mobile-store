// Compatibility entry. SettingsPage consumes ../utils/settingsFeaturePolicy for settings-tab and runtime-badge decisions.
import { SettingsPage } from './settings/index';

const Settings = SettingsPage;

export default Settings;
